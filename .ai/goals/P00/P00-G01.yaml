id: P00-G01
phase: P00
title: Repository Foundation
state: ACTIVE
objective: Create implementation repository structure, CI, Project Atlas bootstrap, dependency
  policy and Command Code adapter.
constraints:
- Follow Project Atlas authority order
- Do not silently weaken acceptance
- Update canonical documentation when behavior/architecture changes
non_goals: []
dependencies: []
acceptance:
- Repository builds from clean checkout
- CI runs lint/typecheck/tests
- Project Atlas validation passes
- Command Code agents/skills are discoverable
- Architecture docs are linked from ATLAS
gates:
  build: required
  tests: required
  review: required
  documentation: required
evidence: []
history:
- '2026-08-10: reclassified DONE -> ACTIVE after an implementation audit found acceptance
  criteria that are not met.'
- '2026-08-10: unmet — CI runs lint/typecheck/tests: the Python job runs pytest with working-directory
  backend/, where testpaths does not resolve; pytest exits 5 (no tests collected) and the
  step fails.'
- '2026-08-10: closed — CI runs lint/typecheck/tests: both CI jobs ran from backend/, where
  pytest collected nothing (exit 5) and scripts/ did not resolve. All steps now run from the
  repository root via `uv run --project backend`.'
- '2026-08-10: still blocking — review gate: no independent review has been performed.'
