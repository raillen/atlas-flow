id: P03-G01
phase: P03
title: Orchestration Runtime
state: ACTIVE
objective: Implement Run/Task/Attempt domain, event log, SQLite state and scheduler baseline.
constraints:
- Follow Project Atlas authority order
- Do not silently weaken acceptance
- Update canonical documentation when behavior/architecture changes
non_goals: []
dependencies:
- P01-G01
acceptance:
- Transactional state transitions
- Append-only versioned run events
- Cancel/pause/resume baseline
- Crash recovery fixture passes
- Operational DB is not canonical Goal authority
gates:
  build: required
  tests: required
  review: required
  documentation: required
evidence: []
history:
- '2026-08-10: reclassified DONE -> ACTIVE after an implementation audit found acceptance
  criteria that are not met.'
- '2026-08-10: unmet — Crash recovery fixture passes: Persistence defaults to file::memory:?cache=shared
  and the API instantiates it with no path, so no operational state survives a restart.'
- '2026-08-10: unmet — Transactional state transitions: Attempt records are constructed by
  the Harness but never persisted; Persistence has no save_attempt and the attempts table
  is always empty.'
- '2026-08-10: closed — Persistence is file-backed by default and resolved from config; the
  API no longer runs on an in-memory database.'
- '2026-08-10: closed — Attempts are persisted through their whole lifecycle; state changes
  and their events are written in one transaction.'
- '2026-08-10: closed — Crash recovery closes orphaned tasks and attempts and is idempotent.'
- '2026-08-10: still blocking — review gate: no independent review has been performed.'
