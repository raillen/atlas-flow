id: P06-G01
phase: P06
title: Desktop Modes
state: ACTIVE
objective: Implement Tauri/React shell with Discuss, Plan, Build, Review and Project modes.
constraints:
- Follow Project Atlas authority order
- Do not silently weaken acceptance
- Update canonical documentation when behavior/architecture changes
non_goals: []
dependencies:
- P02-G01
- P05-G01
acceptance:
- Keyboard-navigable primary navigation
- Discuss Project Draft visible
- Plan DAG rendered
- Build live task/agent status visible
- Review evidence/gates visible
- Project canonical docs browsable
gates:
  build: required
  tests: required
  review: required
  documentation: required
evidence:
- review: 'FAILED — auditoria independente em 2026-08-11 (revisor não-Opus 5): bundles Windows/macOS
    não configurados e CI Linux-only (.github/workflows/foundation-ci.yml:16; apps/desktop/src-tauri/tauri.conf.json:32);
    P06 permanece ACTIVE.'
history:
- '2026-08-10: reclassified DONE -> ACTIVE after an implementation audit found acceptance
  criteria that are not met.'
- '2026-08-10: unmet — Plan DAG rendered: DesktopModes.tsx is hardcoded markup with no data
  source.'
- '2026-08-10: unmet — Build live task/agent status visible: same, static mockup.'
- '2026-08-10: unmet — Review evidence/gates visible: same, static mockup.'
- '2026-08-10: unmet — Project canonical docs browsable: not implemented.'
- '2026-08-10: unmet — No REST API exists to expose runs, tasks, events or gates to the UI.'
- '2026-08-10: closed — REST API exposes goals, runs, tasks, attempts, events, gates, evidence,
  discussions and canonical docs.'
- '2026-08-10: closed — Plan, Build, Review and Project screens read real backend state; the
  hardcoded mockups are deleted.'
- '2026-08-10: closed — Primary navigation implements the WAI-ARIA tabs pattern with arrow,
  Home and End keys.'
- '2026-08-10: closed — Run events are broadcast over the AG-UI WebSocket as they commit.'
- '2026-08-10: still blocking — Tauri shell is still the unmodified template: no IPC commands,
  no packaging.'
- '2026-08-10: still blocking — review gate: no independent review has been performed.'
- '2026-08-10: closed — Tauri shell: real IPC commands for backend lifecycle and project root,
  a capabilities file, a Content Security Policy replacing csp: null, generated icons, and
  a deb bundle that was built and smoke-tested. The shell also stops a backend it started
  when the window closes.'
- '2026-08-10: fixed — the crate declared tauri with default-features = false, so it had no
  runtime and could not compile at all; nothing had ever built it.'
- '2026-08-10: still blocking — Windows and macOS bundles are unconfigured and untested; AppImage
  is configured but unverified.'
- '2026-08-10: still blocking — review gate: no independent review has been performed.'
- '2026-08-11: review finding closed — gates are reproducible from a clean checkout. scripts/bootstrap.sh
  builds an environment that works even when the checkout is on a noexec mount (which is what
  made the reviewer''s run fail), and scripts/run_gates.sh runs every gate through one command.'
- '2026-08-11: review finding closed — CI is no longer Linux-only: Python, JavaScript and
  the desktop shell run on ubuntu, macOS and Windows.'
- '2026-08-11: review finding closed — dependency auditing added to CI (pip-audit, pnpm audit,
  cargo audit). It immediately found PYSEC-2026-1845 in pytest 8.4.2; pytest is now 9.x.'
- '2026-08-11: still blocking — release artefacts are unsigned. Signing needs a key and a
  mechanism the project owner has to choose.'
- '2026-08-11: still blocking — the review gate is FAILED. Closing these findings does not
  reopen it; that takes another independent review.'
- '2026-08-11: review finding closed — Windows and macOS bundles configured (nsis, msi, app,
  dmg) with generated .ico and .icns icons; the desktop shell is built and tested on all three
  platforms in CI.'
