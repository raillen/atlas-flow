id: P09-G01
phase: P09
title: Hardening
state: ACTIVE
objective: Harden security, recovery, accessibility, protocol compatibility and performance.
constraints:
- Follow Project Atlas authority order
- Do not silently weaken acceptance
- Update canonical documentation when behavior/architecture changes
non_goals: []
dependencies:
- P06-G01
- P08-G01
acceptance:
- Fault-injection suite passes
- Security high findings closed
- Accessibility acceptance met
- Performance budgets measured
- Supported platform packaging smoke tests pass
gates:
  build: required
  tests: required
  review: required
  documentation: required
evidence:
- review: 'FAILED — auditoria independente em 2026-08-11 (revisor não-Opus 5): CI Linux-only,
    AppImage não verificado, sem auditoria DOM/screen reader, sem cargo build/test no CI e
    sem auditoria de dependências (.github/workflows/foundation-ci.yml; tests/e2e/.gitkeep);
    P09 permanece ACTIVE.'
history:
- '2026-08-10: reclassified DONE -> PLANNED after an implementation audit found acceptance
  criteria that are not met.'
- '2026-08-10: unmet — Depends on P06 and P08, both reopened.'
- '2026-08-10: unmet — Performance budgets measured: no measurement is wired into CI.'
- '2026-08-10: unmet — Supported platform packaging smoke tests pass: no packaging pipeline
  exists; src-tauri is the unmodified template.'
- '2026-08-10: unmet — Accessibility acceptance met: no accessibility test runs.'
- '2026-08-10: still blocking — Depends on P08, which is blocked on model invocation.'
- '2026-08-10: still blocking — No packaging pipeline, no performance measurement in CI, no
  accessibility test run.'
- '2026-08-10: closed — Dependencies P06 and P08 have closed their blocking gaps.'
- '2026-08-10: closed — Fault-injection suite: real runs are driven through timeout, process
  kill, malformed output, disconnect and git conflict, plus recovery from both a graceful
  interrupt and a hard kill. Recovery is idempotent and state survives the process that wrote
  it.'
- '2026-08-10: closed — Security findings: redaction was never invoked and is now applied
  at the runner boundary; custom patterns no longer replace the defaults; the desktop had
  no CSP; validate_git_command forbade the two operations the runtime exists to perform and
  was never wired in — it now refuses publishing and history rewriting, and every git call
  passes through it; two sanitizers that could only have corrupted data were removed.'
- '2026-08-10: closed — Accessibility: colour tokens are data and every pair the screens render
  is checked against WCAG 2.2 AA, which found two real failures (amber and green badges below
  4.5:1, and success/failure indistinguishable in grayscale). Focus ring, reduced motion and
  text scaling are enforced in CSS.'
- '2026-08-10: closed — Performance budgets are measured and asserted: event append p95 ~0.5ms
  against a 20ms budget, and every endpoint the desktop polls p95 under 50ms against a 150ms
  budget.'
- '2026-08-10: closed — Packaging smoke test: scripts/package_smoke.sh builds the deb bundle
  and verifies the binary, desktop entry, icons and control metadata are in it.'
- '2026-08-10: still blocking — Only Linux/deb is verified. AppImage needs network access
  at build time and was not verified; Windows and macOS are unconfigured.'
- '2026-08-10: still blocking — No rendered-DOM accessibility audit and no screen-reader walkthrough.'
- '2026-08-10: still blocking — review gate: no independent review has been performed.'
- '2026-08-11: review finding closed — gates are reproducible from a clean checkout. scripts/bootstrap.sh
  builds an environment that works even when the checkout is on a noexec mount (which is what
  made the reviewer''s run fail), and scripts/run_gates.sh runs every gate through one command.'
- '2026-08-11: review finding closed — CI is no longer Linux-only: Python, JavaScript and
  the desktop shell run on ubuntu, macOS and Windows.'
- '2026-08-11: review finding closed — dependency auditing added to CI (pip-audit, pnpm audit,
  cargo audit). It immediately found PYSEC-2026-1845 in pytest 8.4.2; pytest is now 9.x.'
- '2026-08-11: still blocking — release artefacts are unsigned. Signing needs a key and a
  mechanism the project owner has to choose.'
- '2026-08-11: still blocking — the review gate is FAILED. Closing these findings does not
  reopen it; that takes another independent review.'
- '2026-08-11: review finding closed — rendered-DOM accessibility audit added: axe-core over
  the shell and every screen in jsdom, plus assertions axe cannot make (tab order, panel labelling,
  live regions, accessible names). One test audits the audit.'
- '2026-08-11: review finding closed — cargo build and cargo test run in CI, and the desktop
  shell now has 9 unit tests instead of none.'
- '2026-08-11: review finding closed — tests/e2e held only a .gitkeep; the rendered-DOM audit
  replaces the placeholder with tests that actually run.'
- '2026-08-11: still blocking — AppImage is still unverified: its bundler downloads linuxdeploy
  at build time and needs FUSE, neither of which the build machine had.'
- '2026-08-11: still blocking — no screen-reader walkthrough.'
