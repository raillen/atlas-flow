id: P05-G01
phase: P05
title: Goal Planner and DAG Execution
state: ACTIVE
objective: Implement Goal decomposition contract, DAG validation, worktree isolation, concurrency
  and integration.
constraints:
- Follow Project Atlas authority order
- Do not silently weaken acceptance
- Update canonical documentation when behavior/architecture changes
non_goals: []
dependencies:
- P03-G01
- P04-G01
acceptance:
- DAG cycle detection
- Dependency-aware scheduler
- Parallel tasks use isolated worktrees
- Overlapping write scopes do not run unsafely in parallel
- Integration conflict is explicit
gates:
  build: required
  tests: required
  review: required
  documentation: required
evidence:
- review: 'FAILED — auditoria independente em 2026-08-11 (revisor não-Opus 5): CI Linux-only
    (.github/workflows/foundation-ci.yml:16), ausência de assinatura de artefatos, validação
    local bloqueada por dependências/execução do ambiente; P05 permanece ACTIVE até gates
    reproduzíveis passarem.'
history:
- '2026-08-10: reclassified DONE -> ACTIVE after an implementation audit found acceptance
  criteria that are not met.'
- '2026-08-10: unmet — Parallel tasks use isolated worktrees: worktree.py only formats branch
  and directory names; no git command is ever executed.'
- '2026-08-10: unmet — Integration conflict is explicit: not implemented.'
- '2026-08-10: closed — Worktrees are created, committed and removed through real git commands;
  parallel tasks write to isolated checkouts.'
- '2026-08-10: closed — Integration conflicts are detected with `git merge-tree` and reported
  without touching the target checkout.'
- '2026-08-10: closed — Integration is refused when the target has uncommitted changes, and
  serialized so concurrent merges cannot race on HEAD.'
- '2026-08-10: closed — GoalRunner executes a plan end to end respecting dependencies and
  non-overlapping write scopes.'
- '2026-08-10: still blocking — review gate: no independent review has been performed.'
- '2026-08-11: review finding closed — gates are reproducible from a clean checkout. scripts/bootstrap.sh
  builds an environment that works even when the checkout is on a noexec mount (which is what
  made the reviewer''s run fail), and scripts/run_gates.sh runs every gate through one command.'
- '2026-08-11: review finding closed — CI is no longer Linux-only: Python, JavaScript and
  the desktop shell run on ubuntu, macOS and Windows.'
- '2026-08-11: review finding closed — dependency auditing added to CI (pip-audit, pnpm audit,
  cargo audit). It immediately found PYSEC-2026-1845 in pytest 8.4.2; pytest is now 9.x.'
- '2026-08-11: still blocking — release artefacts are unsigned. Signing needs a key and a
  mechanism the project owner has to choose.'
- '2026-08-11: still blocking — the review gate is FAILED. Closing these findings does not
  reopen it; that takes another independent review.'
