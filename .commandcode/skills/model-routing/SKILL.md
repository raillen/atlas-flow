---
name: model-routing
description: Apply Atlas Flow development model routing and fallback rules.
---

# Model Routing
Read `.ai/orchestration/model-policy.yaml`, `role-routing.yaml` and `fallbacks.yaml`. Probe `cmd --list-models`. Use DeepSeek/MiMo as primary roster; use Luna when available for efficient roles. Enforce cross-provider review for high-risk work when possible.
