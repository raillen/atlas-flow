# Command Code — Start Atlas Flow Development

Use this in a fresh Command Code session:

> Recover Atlas Flow project context using `ENTRYPOINT.md`, `PROJECT_STATE.md`, `docs/ATLAS.md` and the active Goal. Follow root `AGENTS.md`, `.commandcode/AGENTS.md`, Project Atlas authority order, and `.ai/orchestration/` policies. Do not implement anything outside the active Goal. First report the Goal, relevant dependencies, canonical documents, proposed validation commands, and any blocker. Then proceed according to the current autonomy policy.

Before choosing a model for an automated role, run `cmd --list-models` and follow `.ai/orchestration/role-routing.yaml`.
