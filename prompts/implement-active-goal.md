# Implement Active Goal

1. Read `PROJECT_STATE.md` and active Goal.
2. Load only relevant context through ATLAS/query packs.
3. Confirm Goal is executable and dependencies are satisfied.
4. Produce a task DAG with declared write scopes and gates.
5. Select Agent role/Skills and route model by policy.
6. Use an isolated worktree for mutable work.
7. Implement narrowly.
8. Run deterministic gates.
9. Request cross-provider review when required.
10. Attach evidence and update canonical docs/state.
11. Never mark DONE without required evidence.
