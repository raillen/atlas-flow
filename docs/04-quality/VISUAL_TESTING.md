# Visual Testing

Current preferred roster is text-oriented; visual correctness must not depend on an LLM.

Baseline: component snapshots where useful, screenshot regression, overflow/geometry checks, accessibility tree checks and human polish review.

Optional vision models can be added by policy later without changing gates.
