# Reliability and Fault Injection

Inject process kill, ACP disconnect, provider timeout/rate limit, malformed runner output, Git conflict, SQLite contention, cancellation during write and stale Goal revision.

Fail closed when canonical and operational state cannot be reconciled safely.
