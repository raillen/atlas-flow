# Security Testing

Threats: prompt/tool injection, malicious repo content, shell injection, path traversal, secret leakage, MCP trust, permission bypass, destructive Git, terminal/HTML escape injection, supply chain.

Tests cover untrusted tool input, destructive-command hooks, path boundaries, redaction, malformed protocol payloads, permission modes and dependency/SBOM checks.
