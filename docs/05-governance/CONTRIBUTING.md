# Contribution Workflow

Start from Goal/linked issue → read ATLAS impact → isolated branch/worktree → narrow diff → tests → docs → gates → independent review for medium/high risk → evidence → merge.

Do not mix unrelated refactors into a Goal.
