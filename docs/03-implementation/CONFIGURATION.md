# Configuration

Precedence: run override → project orchestration config → Project Atlas manifest/profile → user Atlas Flow settings → defaults.

Secrets never in Git; use OS keychain/secure storage or env refs.

Domains: autonomy, concurrency, budget, model routing, runner discovery, MCP, Git, artifact retention, logging/redaction.
