# Project Atlas Integration

Consume Project Atlas as protocol/dependency, not copied semantics.

Read: manifest, ATLAS, profile, Goals, model policy, registries, compatibility metadata.

Canonical writes go through validated Project Atlas services/commands preserving authority and Goal locks.

On open: detect framework version → validate compatibility → show migration need → never perform incompatible/destructive auto-migration without authorization.
