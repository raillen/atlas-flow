# Running Goals

Select a LOCKED Goal. Atlas Flow shows context, DAG, roles, model routes, gates and budget.

Agentic mode asks plan approval, then executes until human gate/exception. DONE requires evidence.
