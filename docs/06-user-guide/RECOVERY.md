# Recovery

On lost chat/model context, Atlas Flow reloads manifest/state/ATLAS/active Goal and local operational state.

If Atlas Flow is unavailable, another Project Atlas-capable agent can recover from repository starting at `ENTRYPOINT.md`.
