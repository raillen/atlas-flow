# Developing Atlas Flow with Command Code

Start `cmd`; plan with `cmd --plan`; select models with `/model` or `--model`.

Preferred IDs currently expected:
- `deepseek/deepseek-v4-pro`
- `xiaomi/mimo-v2.5-pro`
- GPT-5.6 Luna only if `cmd --list-models` returns an accepted Luna id.

Use `/agents`, `/skills`, root AGENTS.md, and MCP as needed. Do not default to `--yolo`; use isolated worktrees and deterministic hooks before increasing autonomy.
