# Observability

Every task exposes role, model/provider, runner, status, worktree, attempts, elapsed time, token/cost when available, files/diff, tests, transcript, evidence and routing explanation.

First-class queries:
- Why this model?
- Why is this blocked?

Scorecards record first-pass success, repairs, review rejection, CI failures, latency, cost and revert rate.
