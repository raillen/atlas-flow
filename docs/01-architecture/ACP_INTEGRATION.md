# ACP Integration

ACP is preferred between Atlas Harness and compatible coding agents.

Atlas Flow acts as ACP client and should support capability-negotiated initialization, sessions, resume/close, prompt/event streaming, permissions, terminal/file/tool events, cancellation, implementation metadata and MCP forwarding where supported.

Optional capabilities must degrade gracefully.
