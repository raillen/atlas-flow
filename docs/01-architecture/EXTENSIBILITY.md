# Extensibility

Extension points: Runner, provider metadata adapter, Evidence collector, Gate, Planner strategy, Context source, Project Atlas registry package, UI event projection.

Extensions declare API version, capabilities, permissions, platforms and config schema.

Prefer boundary adapters over direct access to scheduler internals.
