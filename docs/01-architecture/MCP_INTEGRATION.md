# MCP Integration

MCP connects agents/runtime to external tools/data such as GitHub, issue trackers, databases, browsers/test systems and documentation systems.

Security: explicit servers, per-role allowlists, secrets outside repo, logged destructive calls, read-only tools for planning roles by default.

MCP does not replace ACP.
