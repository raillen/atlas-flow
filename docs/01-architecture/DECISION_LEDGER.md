# Decision Ledger

States: `PROPOSED | ACCEPTED | REJECTED | SUPERSEDED`.

Fields: id, title, statement, rationale, status, source messages, timestamp, affected domains, supersedes, requires_adr, canonical_targets.

Accepted architecture normally becomes ADR/canonical docs. Rejected ideas remain searchable but non-authoritative. Later choices supersede; they do not silently overwrite.
