# Build UX

Main canvas: live DAG.

Selected task drawer: role, model, runner, worktree, elapsed/cost, transcript, permissions, changed files and retries.

Controls: pause, cancel, retry, escalate model, open worktree, inspect context pack.

Destructive controls require confirmation when not trivially reversible.
